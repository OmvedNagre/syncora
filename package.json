{
  "name": "syncora-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"magenta,cyan\" \"npm run dev:server\" \"npm run dev:client\"",
    "electron": "electron electron/main.js",
    "dev:electron": "concurrently -n \"SERVER,CLIENT,ELECTRON\" -c \"magenta,cyan,yellow\" \"npm run dev:server\" \"npm run dev:client\" \"wait-on http://localhost:5173 && npm run electron\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^33.2.1",
    "wait-on": "^8.0.1"
  }
}
